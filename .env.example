# Optional. Without it, GitHub allows 60 requests/hour; with it, 5000.
# A classic token with NO scopes is enough — Nightshift only reads public data.
# https://github.com/settings/tokens
GITHUB_TOKEN=

# Only needed if you run the agent unattended on a server.
ANTHROPIC_API_KEY=
